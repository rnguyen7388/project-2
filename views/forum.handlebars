<header class="width">
    <h1 class="notify">Notify</h1>
    <h2 class="neighborhood">{{city}}</h2>
    <section id="userInfo">
        <p>@{{username}}</p>
        {{!-- current user ^ --}}
        <p>home: {{neighborhood}}</p>
    </section>
</header>
<main>
    {{!-- <section id="cityInfo">
        {{!-- neighborhood info --}}
    </section> --}}
    <div id="button">
        <button id="changeCity">Change Cities</button>
    </div>
    {{!-- create forum posts --}}
    <div class="width createPost">
        <input id="newTitle" class="width" placeholder="Subject"></input>
        <textarea rows="3" class="width" id="newPost" placeholder="What would you like to tell "
            {{this.neighborhood}}></textarea>
        <button id="postBtn" type="submit">Post</button>
    </div>

    {{!-- post forum posts --}}
    <section class="width forum">
        {{#each post}}
        <p class="title">{{this.title}}</p>
        <div class="username">@{{this.user}}</div>
        <div class="post">{{this.text}}</div>
        <div class="taskBar">
            <div class="likes">
                {{this.likes}}
                <button class="likeBtn">
                    <img src="like.png" alt="Like Icon" class="likeImg">
                </button>
            </div>
        </div>
        {{/each}}
    </section>
</main>
<footer>
    <small>All Rights Reserved © 2020</small>
</footer>